# Current implementation plan

ProjectBoard uses the local Codex CLI architecture and four-state task workflow described in `README.md`, `PRODUCT.md`, and `docs/ARCHITECTURE.md`.

The first version intentionally excludes model/profile selection, custom CLI arguments or paths, other Agent runtimes, remote execution, priority-aware scheduling and complex conflict strategies.
