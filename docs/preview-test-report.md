# 交互预览测试报告

日期：2026-09-05。环境：Windows、外部 Google Chrome、Node.js 本地预览服务。

## Kaneo 框架重做后的补充验收

按用户要求重新对照 Kaneo 官方首页的应用交互演示，完成窄侧栏、完整窗口工作区、顶部路径与页签合并、第二行工具栏、知识三栏布局。原有浏览器测试数据保留。

- 外部 Chrome 桌面检查：看板列、知识树、节点内容和属性栏正常展示。
- 900px 窗口：页面滚动宽度为 900px，工具栏自动换行为 84px，新建任务按钮右边界为 310px，未被裁切。
- 390px 手机：页面滚动宽度为 390px；知识树默认隐藏，点击后正常展开。
- 任务详情：打开后焦点位于“关闭任务”，背景工作区使用 inert 隔离。
- 知识地址 `技术.开发.启动命令` 正常直达；按“技术”标签正确筛出 4 个节点。
- 24 项功能检查在布局重做后重新运行，全部通过。
- 独立审查提出中等宽度工具栏、抽屉焦点、我的工作无效看板切换三项问题，均已处理。
- 设计检测产生两项字号和一项覆盖层阴影的文档提示，已补充到 DESIGN.md 和设计 sidecar。

下方保留本轮前半段数据流程验收记录；首版窄屏测量值仅用于记录当时版本，以本补充节为当前框架依据。

## 部署与账户

- 地址：http://127.0.0.1:4173
- 启动文件：`preview/serve.cjs`；部署保留运行，Chrome 留在知识树页面。
- 预览账户：`admin / admin123`。默认进入工作空间；退出后可验证登录。
- 数据：浏览器 localStorage；实际浏览器测试创建的数据保留，并导出到 `preview/browser-test-data.json`。
- 预览 HTML、样式、图标及脚本均自包含，无 CDN 依赖。

## 测试清单与结果

| 测试条目 | 验证方式 | 结果 |
| --- | --- | --- |
| 首页和 index.html 可访问 | HTTP | 200，HTML 内容类型正确 |
| 健康检查 | HTTP /health | 200，返回 preview 模式 |
| 不存在路径和项目文件访问 | HTTP | 404，不暴露设计文件 |
| 知识树初始展示与精确地址跳转 | 外部 Chrome | 通过，技术.开发.启动命令可直达 |
| 创建有内容的父节点及无标签子节点 | 外部 Chrome | 通过，子节点不继承标签 |
| 标签筛选与批量删除影响清单 | 外部 Chrome | 通过，筛选 1 个父节点，明确列出 2 个待删除节点 |
| 子树删除与回收站恢复 | 外部 Chrome | 通过，节点及子节点整体恢复 |
| 从知识创建任务 | 外部 Chrome | 通过，自动保留知识引用 |
| 任务状态、检查清单、工作记录 | 外部 Chrome | 通过，状态更新为进行中，记录持久化 |
| 任务记录写回知识 | 外部 Chrome | 通过，选择节点后继续编辑并保存 |
| 知识节点移动、重命名和任务引用 | 外部 Chrome | 通过，地址更新为技术.开发.已验证子节点，任务关联保持 |
| 列表关键词筛选 | 外部 Chrome | 通过，输入验收只显示对应任务 |
| 错误密码与正确密码登录 | 外部 Chrome | 通过，错误密码显示提示，admin123 可进入 |
| JSON 备份导出 | 外部 Chrome、文件检查 | 通过，生成 11,171 字节备份，包含 2 项目/8 任务/14 节点 |
| JSON 备份导入与恢复 | 外部 Chrome | 通过，展示正确数据数量，确认后显示备份已恢复 |
| 桌面视觉布局 | 外部 Chrome 截图 | 检查知识树、工作台层级与编辑区 |
| 390px 窄屏布局 | 外部 Chrome 视口与 DOM | 通过，页面宽度与滚动宽度均 375px；看板自身宽 345px、内容宽 1014px，滚动限制在看板内 |
| 浏览器重新加载后数据保留 | 外部 Chrome | 通过，多次重载保留测试节点和任务 |
| 页面控制台 | 外部 Chrome | 检查时无 error/warn |

## 功能级检查

`node preview/check.cjs`：24/24 通过，原始结果见 `preview/test-results.json`。

检查在独立 Node VM 中运行，不使用真实浏览器存储，也不会清理保留的浏览器测试数据。覆盖名称/同级唯一约束、子树移动后稳定引用、多标签 AND/OR、批量增删标签、标签合并去重、删除范围去重、恢复、内容历史、双向关系、自定义状态迁移、任务记录写回知识、备份循环/重复 ID 拒绝、内容转义和并发版本保护。

## 调整与限制

- 记录写入知识时，先关闭选择对话框，再打开内容编辑对话框，保证流程连续。
- 写入成功后推进本地修订号，保存失败时仍能重试。
- 筛选改为输入后更新，并恢复输入位置。
- 备份导入使用页面内文件选择控件，已完成真实文件导入和恢复验证。浏览器自动化文件选择曾超时，调整后通过。
- 完整安装、Go/SQLite、服务端鉴权、密码修改、HTTPS、升级迁移与正式性能目标不属于已验收项目；原型中展示相应边界。
- 永久删除与密码修改未在用户浏览器实际执行。标签合并、状态管理等由功能级检查覆盖，不将其标为完整浏览器验收。
- 看板拖动已提供交互实现；本轮浏览器验证了状态选择和列表，未完成鼠标拖放的端到端验收。
# 项目标签与 MCP 补充验收（2026-09-05）

- Node VM 功能检查：28 / 28 通过。新增项目同名标签隔离、任务/节点统一改名删除、混合批量操作、子树删除影响预告、MCP 演示范围校验与标签读写。
- 浏览器验证：标签已进入项目页签，任务类型筛选得到正确结果；MCP list_tasks 返回当前项目任务，传入其他项目 ID 显示 PROJECT_SCOPE_MISMATCH。
- 响应式：390px 宽度下页签、筛选与批量操作可见，页面 scrollWidth 为 390，无横向溢出。
- MCP 验证范围为浏览器模拟调用，尚未测试真实协议、客户端连接或服务端持久化。正式接入验收清单见 mcp-design.md。
# 独立任务页与可选字段验收（2026-09-05）

- Node VM 检查：33 / 33 通过。新增标题单独创建、业务字段默认空、逐字段设置/清空、0% 与未设置区分、日期/颜色/关联备份校验，以及页面导航无残留遮罩。
- 浏览器创建 PB-9“验收：按需设置任务字段”：初始无属性；设置紧急优先级后刷新仍保留；清空优先级后隐藏；设置 0% 进度正确显示。返回项目后浏览器后退恢复同一任务 URL。
- 390px 手机宽度：正文与属性纵向排列，scrollWidth=390，无横向溢出；浏览器 error/warn 日志为空。
- 提醒与重复周期仅验收配置保存，未实现定时触发。真实 MCP、服务端路由与 SQLite 仍待正式服务实现。
# 评论附件与时间线验收（2026-09-05）

- Node VM：37 / 37 通过，新增评论展示、文字/附件发布、回复、编辑、删除恢复、编码与备份校验、时间线区间与未安排日期不写入检查。
- 浏览器在 PB-9 上传 comment-upload-test.txt 并发表评论，刷新后评论和附件保留。下载文件与原文件 SHA256 一致：6BDF52646B5A486EF2706B79677EBBD0CA3189CAFDC8777F52BA837375D51838。
- 时间线实际显示 PB-9 的 9 月 5–10 日区间及 PB-1 的截止标记；切换到 10 月显示范围外数量，未安排任务继续显示。
- 390px 窄屏下时间线仅在日期区横向滚动，页面 scrollWidth=390；评论输入、文件卡片与操作按钮可用。
- 实际浏览器上传验证使用文本文件；图片内嵌、拖放和粘贴实现已加入，但本轮未逐项执行浏览器验收。预览附件限制与正式本地存储方案见 comments-and-timeline.md。
# 创建状态与移除待处理（2026-09-05）

38 / 38 功能检查通过，包含默认创建、旧空状态/待处理状态迁移、回收站迁移、幂等迁移和初始状态保护。浏览器已显示创建（5）、进行中（3）、已完成（2）、已取消（0），筛选不再包含待处理或未设置状态。
# 固定任务详细与附件（2026-09-05）

40 / 40 功能检查通过，新增固定区域顺序、详细文字/附件与评论独立保存、附件备份及非法附件拒绝。浏览器在 PB-9 上传 PNG 测试文件并保存，刷新后图片 naturalWidth=1（与 1px 测试图一致），任务详细位于评论上方，原评论保留。
# 任务详细阅读状态与属性入口（2026-09-05）

任务详细默认阅读，编辑后可保存或取消；移除重复添加字段按钮，保留属性标题旁的加号。41 / 41 功能检查通过，覆盖阅读/编辑切换、取消不改数据、保存返回阅读状态。
