import{afterEach,expect,it}from'vitest';import{fixture}from'./helpers.js';import{WorkItems}from'../src/server/work-items.js';let cleanup=()=>{};afterEach(()=>cleanup());it('atomically materializes two-level children and dependencies',()=>{const f=fixture('split');cleanup=f.cleanup;const s=new WorkItems(f.db);const p:any=s.create(f.actors.developer,{requestId:'split-create-1',projectId:f.ids.project,title:'Parent',descriptionMarkdown:'',acceptanceCriteriaMarkdown:'all'});const result:any=s.decompose(f.actors.developer,p.id,{requestId:'split-submit-1',expectedVersion:p.version,items:[{key:'a',title:'A',acceptanceCriteriaMarkdown:'A done'},{key:'b',title:'B',acceptanceCriteriaMarkdown:'B done'}],dependencies:[{from:'b',to:'a'}]});expect(result.children).toHaveLength(2);expect(f.db.get<any>('SELECT COUNT(*) n FROM work_item_dependencies')?.n).toBe(1);expect(()=>s.decompose(f.actors.developer,result.children[0].id,{requestId:'split-submit-2',expectedVersion:result.children[0].version,items:[{key:'a',title:'A',acceptanceCriteriaMarkdown:'x'},{key:'b',title:'B',acceptanceCriteriaMarkdown:'x'}],dependencies:[]})).toThrow(/cannot be split/) });
