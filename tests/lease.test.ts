import{afterEach,expect,it}from'vitest';import{WorkItems}from'../src/server/work-items.js';import{fixture}from'./helpers.js';
let cleanup=()=>{};afterEach(()=>cleanup());it('enforces one active lease per agent and invalidates released leases',()=>{const f=fixture('lease');cleanup=f.cleanup;const s=new WorkItems(f.db);const one:any=s.create(f.actors.developer,{requestId:'lease-create-01',projectId:f.ids.project,title:'One',descriptionMarkdown:'',acceptanceCriteriaMarkdown:'ok',assigneeKind:'agent',assigneeId:f.ids.agent});const two:any=s.create(f.actors.developer,{requestId:'lease-create-02',projectId:f.ids.project,title:'Two',descriptionMarkdown:'',acceptanceCriteriaMarkdown:'ok',assigneeKind:'agent',assigneeId:f.ids.agent});const lease:any=s.claim(f.actors.agent,one.id,'lease-claim-001',one.version);expect(lease.expiresAt).toBeTruthy();expect(()=>s.claim(f.actors.agent,two.id,'lease-claim-002',two.version)).toThrow(/active lease/);expect(s.heartbeat(f.actors.agent,lease.runId,lease.id).valid).toBe(true);s.release(f.actors.agent,one.id,lease.id,'done');expect(()=>s.heartbeat(f.actors.agent,lease.runId,lease.id)).toThrow(/no longer valid/)});
