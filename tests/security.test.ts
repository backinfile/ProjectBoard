import{describe,expect,it}from'vitest';import{redact,renderMarkdown,validatePassword}from'../src/server/security.js';
describe('content and secret boundaries',()=>{it('sanitizes untrusted markdown and protocols',()=>{const html=renderMarkdown('<script>alert(1)</script> [bad](javascript:alert(1)) [ok](https://example.com)');expect(html).not.toContain('<script');expect(html).not.toContain('javascript:');expect(html).toContain('noopener noreferrer')});it('redacts nested credentials and bearer values',()=>{const value:any=redact({token:'plain',nested:{Authorization:'Bearer secret',note:'Bearer abc.def'}});expect(JSON.stringify(value)).not.toContain('plain');expect(JSON.stringify(value)).not.toContain('secret');expect(JSON.stringify(value)).not.toContain('abc.def')});it('enforces temporary password policy',()=>{expect(validatePassword('short')).toBe(false);expect(validatePassword('long-password-123')).toBe(true)})});
