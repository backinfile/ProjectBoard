# ProjectBoard 产品说明

ProjectBoard 是人类成员与本地 Codex Agent 共用的项目协作空间。任务负责推进工作，知识库负责保留项目长期事实，Agent 需求负责承载每一次 AI 执行。

## 产品边界

- Agent 运行时仅支持本地 Codex CLI。
- 不提供 SSH/MCP Agent、外部 Runner、自定义 CLI 路径、向量检索或知识图谱。
- Git 仓库通过 GitHub App 授权。
- 每个项目只有一个树状知识库；SQLite 关键词搜索足够支撑首版。
- 所有 Agent 行为必须先成为一种已知类型的 Agent 需求，没有自由创建入口。

## 主要流程

1. 成员创建任务，选择人工处理或请求 Agent 计划/执行。
2. 调度器原子领取一个排队需求，启动全新 Codex 会话并记录全过程。
3. 计划批准、失败重试和任务返工都创建新需求，不恢复原会话。
4. 任务执行成功后按配置停在“完成”或自动“关闭”。
5. 每次任务关闭都创建任务知识整理需求；它通过受控结构化操作更新正式知识树。
6. 达到项目阈值后创建全局整理需求，合并重复、过期和分散知识。

知识节点有完整历史。开发者可以锁定节点来保护稳定约束；锁只限制 Agent，不限制成员，也不向子节点继承。
