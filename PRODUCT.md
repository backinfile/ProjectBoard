# ProjectBoard 产品说明

ProjectBoard 是成员与服务器本地 Codex Agent 共用的项目协作空间。任务推进工作，知识库保存长期事实，Agent 需求承载每一次 AI 执行，通知与审计让协作过程可追踪。

## 用户与权限

- 系统角色分为管理员和普通用户。管理员管理用户、本地 Agent、项目、系统设置和全局审计。
- 项目成员角色分为开发者和查看者。开发者可修改任务和知识，查看者仅可读取有权限的项目。
- 用户可以维护个人资料、修改密码、查看并撤销自己的会话。
- 任务支持成员或 Agent 负责人、关注者、`@` 提及和相关通知。

## 核心工作区

项目工作区包含任务看板、知识库、Agent 需求和我的消息。设置中心包含我的账号、本地 Agent、用户管理、项目管理、审计和系统服务六个页签；非管理员只显示自己的账户设置。

任务支持 Markdown 描述与验收条件、四种优先级、标签、父子任务、依赖、阻塞原因、讨论记录和附件。标准流程为 `created → in_progress → completed → closed`；简单对话流程为 `created → in_progress → closed`，工作流类型在任务创建后不可修改。

## Agent 协作模型

1. 成员创建任务，选择人工处理，或请求 Agent 先计划/直接执行。
2. 每次计划、执行、计划批准、重试、返工、合并和知识整理都会创建独立的一次性 Agent 需求。
3. 标准任务在隔离 worktree 中实现，再在项目主仓库中通过 `--no-ff` 合并；简单对话任务直接在目标分支工作。
4. 任务关闭时自动创建任务知识整理需求；达到项目天数或数量阈值后创建全局知识整理需求。
5. 原始 JSONL、最终消息、错误、请求状态和重试谱系永久保留为执行证据。

Agent 可配置 Codex 模型、思考强度、并发容量、单轮超时、接取标签和拒绝标签。模型配置会在需求开始时固化，执行结束后保存输入、缓存输入、输出、思考和总 Token 用量。拒绝标签优先；每个需求独立选择可用 Agent，没有匹配项时继续排队。失败或中断不会触发自动 reset、stash 或 Git 网络操作。

## 知识模型

每个项目只有一棵知识树。节点保存 Markdown、父子关系、排序、乐观版本、Agent 锁和完整修订历史，并支持关键词搜索、移动和恢复。

知识 Agent 只能提交完整的结构化节点操作，由服务端事务性校验。锁只限制 Agent 修改当前节点，不限制成员，也不会自动锁定子节点。当前知识内容直接保存在 SQLite 中，不支持节点附件、外部文件引用、向量检索或知识图谱。

## 产品边界

- Agent 运行时仅支持服务进程调用本地 `codex` CLI。
- 不提供 SSH、MCP Agent、外部 Runner、远程执行、自定义 CLI 路径或任意 CLI 参数；模型和思考强度是受控的 Agent 级配置。
- 项目绑定唯一且不可修改的服务器本地 Git 仓库绝对路径。
- 不配置 Git 托管平台、不保存 Git 凭据，也不自动 `fetch`、`pull` 或 `push`。
- 项目主仓库写入按项目串行；标准任务 worktree 在关闭后由成员显式清理。
- Schema v10 可从 v9 原地迁移；更早的 schema 仍不兼容，也不提供旧 API 兼容层。
