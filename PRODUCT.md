# ProjectBoard 产品说明

ProjectBoard 是成员与服务器本地 Codex Agent 共用的任务协作空间。项目直接绑定服务器文件夹中的 Git 仓库，不依赖 GitHub 或其他 Git 提供商认证。

## 产品边界

- Agent 仅支持服务进程直接调用本地 `codex` CLI。
- 不提供 SSH、MCP Agent、外部 Runner、远程执行或自定义 CLI 路径。
- 项目路径是不可修改的服务器绝对路径；缺失的文件夹和 Git 仓库会在项目创建时初始化。
- 系统不自动 fetch、pull 或 push，不保存 Git 凭据。
- 每个任务保留对话、附件、关注者、标签、审计事件和 execution 原始证据。

## 工作流程

- 标准流程为默认值：`创建 → 进行 → 完成 → 关闭`。实现发生在任务 worktree，完成阶段由独立 Agent 调用合并回目标分支。
- 简单对话流程：`创建 → 进行 → 关闭`。成员或 Agent 直接操作主仓库。
- 工作流程只能在创建任务时选择。
- 计划暂停会在实现前等待确认；完成前暂停在标准流程中表示合并审查，在简单流程中表示关闭审查。

## 调度规则

Agent 可设置并发容量、单轮超时、接取标签和拒绝标签。接取标签为空时接受全部任务；非空时任务至少匹配一个。拒绝标签命中即排除，并且优先于接取标签。每次调用独立选择 Agent，没有匹配项时任务继续排队。

标准流程的实现调用与合并调用恢复同一个 Codex session。主仓库写入按项目串行；失败时保留现场，不自动 reset 或 stash。

Schema v8 与旧数据、旧 API 和旧执行模型均不兼容，不提供迁移。
