# ProjectBoard 产品说明

ProjectBoard 是成员与服务器本地 Codex Agent 共用的项目协作空间。任务负责推进工作，知识库负责保留项目长期事实，“Agent 需求”负责承载每一次 AI 执行。项目直接绑定服务器文件夹中的 Git 仓库，不依赖 GitHub 或其他 Git 提供商认证。

## 产品边界

- Agent 运行时仅支持服务进程直接调用本地 `codex` CLI。
- 不提供 SSH、MCP Agent、外部 Runner、远程执行、自定义 CLI 路径、向量检索或知识图谱。
- 项目路径是不可修改的服务器绝对路径；缺失的文件夹和 Git 仓库会在项目创建时初始化。
- 系统不自动 fetch、pull 或 push，也不保存 Git 凭据。
- 每个项目只有一棵树状知识库，首版使用 SQLite 关键词搜索。

## 工作流

1. 成员创建任务，选择人工处理或请求 Agent 计划/执行。
2. 每次计划、执行、重试、返工、合并和知识整理都形成独立的一次性 Agent 需求，并保留原始结果。
3. 标准任务在隔离 worktree 中实现，再在项目主仓库中通过 `--no-ff` 合并；简单对话任务直接在目标分支工作。
4. 任务关闭时自动创建任务知识整理需求；达到项目阈值后创建全局知识整理需求。
5. 知识 Agent 只能提交结构化节点操作，由服务端事务性校验版本、锁和项目归属。

知识节点保留完整历史，内容直接保存在节点中，不支持附件或外部文件引用。开发者可以锁定节点以保护稳定约束；锁只限制 Agent 修改当前节点，不限制成员，也不向子节点继承。

## 调度规则

Agent 可设置并发容量、单轮超时、接取标签和拒绝标签。拒绝标签优先于接取标签；每次需求独立选择可用 Agent，没有匹配项时继续排队。对项目主仓库的写入按项目串行，失败时保留现场，不自动 reset 或 stash。

Schema v9 是两个功能分支合并后的统一模型，与旧数据、旧 API 和旧执行模型不兼容，不提供迁移。
